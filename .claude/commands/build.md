Run `python3 build.py` to regenerate all pages in `public/` from the Jinja2 templates.

After running, report how many HTML files were generated and confirm the count matches the 13 expected pages:
index, about-us, calendar, cadet-mess, enrollment, gallery, general-knowledge, grooming-uniform,
meet-the-officers, parents, physical-fitness, programs, 404.
