Start the local development server by running `python3 scripts/serve_local.py`.

The server runs on port 8000. The site will be available at http://localhost:8000.

Note: The Content Security Policy defined in `netlify.toml` is NOT enforced locally.
To test CSP-sensitive features (gallery Google Drive loading, enrollment form submission),
use a Netlify deploy preview instead.
